

# Find out which shell is currently being used
grep $USER /etc/passwd

# Find out which shells are currently installed
cat /etc/shells

# systemctl - Control the systemd system and service manager
man systemctl

# Get main information about the system
# sudo apt install screenfetch
screenfetch

# Shutdown at a specific time
sudo shutdown 21:00
sudo shutdown +15 #(minutes)

