



@REM cd # The cd command is the shorthand version of the chdir command.

@REM cmd # The cmd command starts a new instance of the cmd.exe command interpreter.

@REM copy # The copy command does simply that — it copies one or more files from one location to another.

@REM date # The date command is used to show or change the current date.

@REM del # The del command is used to delete one or more files.

@REM dir # The dir command is used to display a list of files and folders contained inside the folder that you are currently working in.

@REM echo # The echo command is used to show messages, most commonly from within script or batch files.

@REM edit # The edit command starts the MS-DOS Editor tool which is used to create and modify text files.

@REM exit # The exit command is used to end the cmd.exe (Windows) or command.com (MS-DOS) session that you're currently working in.

@REM findstr # The findstr command is used to find text string patterns in one or more files.

@REM for # The for command is used to run a specified command for each file in a set of files.

@REM ftp # The ftp command can be used to transfer files to and from another computer.

@REM help # The help command provides more detailed information on any of the other Command Prompt or MS-DOS commands.

@REM hostname # The hostname command displays the name of the current host.

@REM if # The if command is used to perform conditional functions in a batch file.

@REM ipconfig # The ipconfig command is used to display detailed IP information for each network adapter utilizing TCP/IP. The ipconfig command can also be used to release and renew IP addresses on systems configured to receive them via a DHCP server.

@REM md # The md command is the shorthand version of the mkdir command.

@REM mem # The mem command shows information about used and free memory areas and programs that are currently loaded into memory in the MS-DOS subsystem.

@REM mkdir # The mkdir command is used to create a new folder.

@REM mofcomp # The mofcomp command properly displays the data within a Managed Object Format (MOF) file.

@REM more # The more command is used to display the information contained in a text file.

@REM mount # The mount command is used to mount Network File System (NFS) network shares.

@REM mountvol # The mountvol command is used to display, create, or remove volume mount points.

@REM move # The move command is used to move one or files from one folder to another.

@REM netstat # The netstat command is most commonly used to display all open network connections and listening ports.

@REM nslookup # The nslookup is most commonly used to display the hostname of an entered IP address.

@REM pause # The pause command is used within a batch or script file to pause the processing of the file. When the pause command is used, a "Press any key to continue…" message displays in the command window.

@REM ping # The ping command sends an Internet Control Message Protocol (ICMP) Echo Request message to a specified remote computer to verify IP-level connectivity.

@REM print # The print command is used to print a specified text file to a specified printing device.

@REM mkdir # The rmdir command is used to delete an existing or completely empty folder.

@REM set # The set command is used to display, enable, or disable environment variables in MS-DOS or from the Command Prompt.

@REM shutdown # The shutdown command can be used to shut down, restart, or log off the current system or a remote computer.

@REM sort # The sort command is used to read data from a specified input, sort that data, and return the results of that sort to the Command Prompt screen, a file, or another output device.

@REM telnet # The telnet command is used to communicate with remote computers that use the Telnet protocol.

@REM time # The time command is used to show or change the current time.

@REM timeout # The timeout command is typically used in a batch or script file to provide a specified timeout value during a procedure. The timeout command can also be used to ignore keypresses.

@REM tree # The tree command is used to graphically display the folder structure of a specified drive or path.

@REM waitfor # The waitfor command is used to send or wait for a signal on a system.

@REM where # The where command is used to search for files that match a specified pattern.

@REM whoami # The whoami command is used to retrieve user name and group information on a network.

@REM xcopy # The xcopy command can copy one or more files or directory trees from one location to another. The xcopy command is generally considered a more "powerful" version of the copy command through the robocopy command trumps even xcopy.


